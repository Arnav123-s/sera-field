# Source alignment before implementation

I read the complete supplied “SERA: The Architecture of True Understanding and Imagination” proposal. The immutable original and hash are retained locally in `intake/`; its scientific propositions are evaluated as hypotheses. The user's direct request establishes the goal and preservation requirement.

## Contracts that make the proposal executable

1. **Physical intervention and gauge transformation.** For an adjoint field, `X_i -> G_i X_i G_i†`; a link transforms as `U_ij -> G_i U_ij G_j†`. Traces of correctly transported products are invariant. Changing mass changes a physical scalar and its consequences. It is not a gauge transformation. I implement and test both operations separately.
2. **Boundary and persistence.** Transient situations and imagined branches are explicit immutable values; learned weights are persistent. Read-only imagination is enforced by tests and fingerprints. Geometric language alone is not the enforcement mechanism.
3. **Topology and verification.** A stable invariant can encode a stable state without establishing that the encoded proposition is true. I retain independent evidence, assumptions, replay and revocable qualification. I do not replace empirical or algebraic checking with a topology label.
4. **Flow and representation growth.** The source does not specify a discretized action, regulator, projection maps, boundary conditions or a dimension-creation operator. Wetterich flow evolves a specified effective action. FIELD-001 tests explicit, function-preserving neural expansion and a fixed-capacity control; this is not renamed exact renormalization flow.
5. **Dynamics.** In the Euclidean convention the cited `D_nu F_nu_mu` flow descends the Yang–Mills action. Smoothness does not make it nondissipative. The finite learner uses an explicit state transition and reports its errors.
6. **Credit.** Curvature is not by definition a loss derivative or eligibility trace. FIELD-001 uses checked outcome credit and ordinary measured optimization. A local eligibility replacement will need a derivative comparison and matched learning experiment.
7. **Identifiability.** Coordinate trajectories identify acceleration, not mass and force separately. The acquisition protocol records measured force and mass calibration. Inverse mass inference is conditional on those controls. Unknown force leaves an equivalence class and is reported that way.
8. **Resources and controls.** The source's 35,840-byte number refers to a retained state, not total training process memory. I separately report parameters, persistent bytes, per-situation state, actual temporary allocations and supervised process-tree memory. No padded unused weights are used to manufacture parameter matching.

## Primary-source checks

The lattice gauge work supplies covariant operations for gauge-theory data, not a theorem connecting grammar to semantics. V-JEPA 2 includes an action-conditioned model and robot planning, so its blanket exclusion from planning in the proposal is not adopted. E-prop supplies a derivative-based eligibility construction. Dynamic network expansion is an established classical control. The hypotheses are tested against these actual mechanisms without assuming that terminology establishes superiority.

The intended separation of imagination and retained knowledge, shared cross-use test, omitted-mechanism challenge, independent checking and preservation of failed results are retained. Every further departure and its experimental consequence will appear in the post-training audit.
