# Preserved diagnosis and engineering repairs

The matched teaching, original finals, independent final replay and persistent
delivery all completed. No final-based parameter change or retraining occurred.

## Audit import repair

`joint020-audit-001` completed its receipt, outcome and reward checks, then stopped
at the additional broader development diagnostics because `assess` was missing
from its imports. I added that import and resumed the audit in the fresh
`joint020-audit-002` attempt. Earlier completed phases are checked and skipped.
The failed attempt and its cost remain in the ledger. No learner, data, protocol,
selection or final predictions changed.

## Chronology/family design issue

The source's original mixed-episode schedule uses physical episode parity for
linear/quadratic drag and also for the placement of the human premise. Those two
variables are therefore confounded. I identified this by inspecting the data
constructor while the final assessment was running. The entire completed course
is preserved as specified. A fixed-weight development diagnosis crosses both
orders for the same worlds. This additional design-quality review is explicitly
separate from the frozen performance gates and prevents broad promotion of the
candidate. The repair requires counterbalanced teaching and a fresh reserved
cohort, rather than optimizing against the opened final.
