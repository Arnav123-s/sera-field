# Reconciliation and new source intake

The local field lab was clean at
`92f75b75754a29746947af8eafecf8c1c8c799f1`. The current qualified owner is
CONCEPT-011, weight identity
`afa6bea53feb6e6b74d98ea52195ff531d21cc22e3863e8eb077f21fba007ba7`.
The reviewed public snapshot and its successful Linux CI remain preserved.

No Python process was returned by the bounded `Get-Process` check, and the shared
numerical lease was absent. The broader CIM inventory was unavailable (access
denied); no job was stopped. Every new numerical job independently acquires the
exclusive lease and an enforced one-core, 2 GiB Windows job object.

The new attachment is 35,528 bytes, SHA-256
`5764e1e09f0375085b5e73697f6f5a4fbaf896e6f6d5437a500d6f36044e50ff`.
It has the same twelve-section proposal with citation/formatting changes and a
59-link bibliography. Its normalized alphabetic word sequence is 98.58% similar
to the preceding synthesis; it is not a byte duplicate. Both copies and manifests
are retained privately. Equations lost in the paste are recovered only from actual
earlier source text, as recorded in the ECHO-007 equation audit.

The new whole-architecture checklist replaces the assumption that a completed
five-use study closes the research. Current qualified behavior remains available.
The new candidate must pass prospective integration, retention and replay gates.

Initial verification: the first test invocation failed during collection because
new modules were initially placed at the repository root. The modules were moved
into the existing Python package. All five mathematical/coupling checks then
passed; the expanded six-test integration check also passed, including a real
training update, independently graded capture decision and exact state resume.
Failed attempt logs and costs remain in `runs/`.
