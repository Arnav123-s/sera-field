# Unified memory source-to-execution map

This maps the latest supplied architecture to the actual connected implementation.
The 141-test baseline passed before teaching. The completed
[report](REPORT.md) and [independent audit](QUALIFICATION.json) now record the
frozen decision. All source, split, mathematical, teaching, replay and protected
parent checks passed. Selection retained step zero; useful learned updating and
positive qualified-history delivery remained unmet. The trained states and full
costs remain available. This map distinguishes implemented paths from useful effects.

| Source proposal | Concrete finite implementation | Evidence required through the actual owner |
|---|---|---|
| Bivalent epigenetic memory | Opposing support/opposition traces remain separate; fast and slow vector traces exchange information bidirectionally | Independent query improvement, retention and correction, in addition to distinct trace values |
| Time-scale separation | Learned rate parameters with slow rate strictly below the fast rate throughout their allowed range | Compare the trained procedure with the stated equal-rate inference control |
| Active, nonreciprocal phase memory | Two conserved fields use antisymmetric cross-coupling, explicit reservoir writes and semi-implicit interfacial relaxation | Mass accounting, passive energy, spectral length, reconstruction and independent answer quality; reciprocal control retained |
| Gauge-aware consolidation | A context-conditioned connection generator defines an exact linear vector-fiber continuous flow | Local-frame covariance, inverse, log Jacobian and gradients passed; task benefit is measured separately |
| Holographic error-correcting representation | One five-qubit perfect-tensor cell per retained scalar, with an explicit encoding and syndrome recovery | All single-Pauli errors and declared two-site erasures passed mathematical checks; delivery checks the recovered answer and source/goal record |
| Intrinsic historical influence | The recalled state modifies the existing situation boundary, before curvature, covariance and temporal computation | The independent query reads that actual field; there is no new standalone answer model |
| Autonomous correction from experience | A support-driven state proposal is graded on a separate human query; the current owner and performed proposal must match the evidence | Actual retained state, signed progress, original goal, duplicate/stale rejection and exact restart |
| Protected valid simple knowledge | Earlier parameters and owners stay preserved, and task-scoped history activates only for its qualified original goal | Empty-scope regression and other-goal identity, plus retained evidence after code recovery |

The biology motivates computational variables; the implementation does not label
them as literal chromatin or quantum cell fates. The conditional flow's density
contract holds with its context fixed. The code protects a specified represented
state under a declared noise channel. Independent supervision establishes whether
that state improved the task, and the recorded source scope controls its reuse.

Primary papers and the precise equations are linked from
[CONTINUOUS_HISTORY_EQUATIONS.md](../../docs/CONTINUOUS_HISTORY_EQUATIONS.md).
The supplied full matrix-group flow, larger holographic constructions,
correlator-derived field actions and broad learned noise regularization retain
their own open entries. Their names do not substitute for code and trained outcomes.
