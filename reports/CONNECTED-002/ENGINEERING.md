# Connected engineering handoff

I reread the original proposal and compared its full mechanism set and the user's
behavior description against the actual trained model, interfaces and evidence.
The row-by-row result is in ../PAPER_COVERAGE.md. FIELD-001 remains preserved.

I added a fresh shared situation owner with byte/numeric inputs, a derived finite
lattice action, input-conditioned Wilson-loop features, transient field evolution,
prediction and learned candidate-choice heads. I connected independently checked
credit to actual owner parameter updates through recorded score-function
eligibilities. Weights, optimizer, credit, pending decisions and random state are
committed in one resumable revision. Mathematical scope and remaining paper-specific
derivations are in ../../docs/CONNECTED_CORE.md.

The final engineering suite passed **55 tests**. It checks derivatives and loop
invariance, live field/input gradients, branch isolation, actual reward effects,
invalid/stale/aliased credit, exact resume, source-group splits and teaching/evaluation
separation. These are implementation tests, not learned broad-capability results.
All supervised logs, including earlier checks during development, accompany this
report. No previous model experiment or final evaluation was restarted.

The human teaching views contain:

| Subject | Teaching | Development | Sealed |
|---|---:|---:|---:|
| Human mathematical problems | 5,918 | 720 | 738 |
| Human programming tasks | 295 | 46 | 33 |
| Human reading questions | 66,880 | 12,410 | 8,116 |
| Total | 73,093 | 13,176 | 8,887 |

There are 193 excluded exact normalized-prompt aliases with retained receipts.
Reading views stay grouped by source article, code views by normalized reference
program, and mathematical views by normalized problem. A further structural/near-
duplicate audit is a campaign gate. These are prospective partitions of the
publishers' training sources, not claims about the publishers' official test sets.
No sealed answer was used to train or select a new owner. Teacher answers remain
separate from prompts; original files and source hashes are unchanged.

Other preserved human dictionary, grammar, conversation, calculus, philosophy and
science sources are identified for qualified adapters. Simulated experience has a
separate origin label. Independent semantic or execution checks remain necessary:
the intake's literal arithmetic and syntax checks do not certify whole-task meaning.

The seven-stage campaign allocates at most 262,144 primary-owner updates per seed
across three fresh seeds, with separate matched mechanism controls. This is a
prospective allocation, not a completed training count. Training must follow the
integration gates, checkpoint/replay requirements, held-out assessments and
plateau diagnoses. All subjects and all paper rows require explicit outcomes.

The delegated workspace is isolated from this source lab and the original SERA.
One numerical CPU thread, the 2 GiB committed process-tree cap and the shared lease
remain in force. There is no elapsed local time cap, paid compute or recurring
monitor. Actual dispatch and completion status are separate from this engineering
report; a work order is not evidence that training has started.
