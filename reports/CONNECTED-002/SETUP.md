# Independent execution setup

The independent clone is D:/ai/agy/sera/labs/sera-field-002 on local branch ag.
Its source engineering commit is 41890e2571cfe96c856b0b0df01410e57529081b.
It has separate Git storage; the only remote is a read-only reference to the source
lab, with a disabled push URL. No GitHub or protected branch was changed.

The clone's local/HANDOFF.json pins the research document, preserved physical
checkpoints, selection record, original SERA owner and human-view files. Its
local/TEACHING_SOURCES.json records exact source paths, identities, rights gates,
runtime and source-group counts. The initial preflight verified all pinned inputs,
branch ag and an unoccupied shared numerical lease.

The first preflight encountered Windows Git ownership checks because the sandbox
uses two user identities. The repair passes safe.directory for the one exact,
user-designated repository to each read-only Git command. No global trust or app
permission settings were changed. The repaired preflight passed; training remains
gated on the subject/goal/checker integrations described in the work order.

Computer Use dispatch status is recorded separately in the source lab's private
local/CONNECTED-002-DISPATCH.json. Worker acceptance and subsequent training state
belong to the clone's local/ACCEPTANCE.json and local/PROGRESS.json. This setup
record alone does not assert that Antigravity has begun or completed training.
