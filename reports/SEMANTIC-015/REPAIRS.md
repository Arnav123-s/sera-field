# Preserved startup repair

Attempt `semantic015-campaign-001` stopped before any update or checkpoint.
Some earlier human teaching sources have multiple views with the same source ID.
The rehearsal selector's heap compared dictionaries after equal priorities/IDs,
raising `TypeError`. I added immutable row position as a deterministic tie-breaker
and a regression check that preserves the distinct views. No final was opened,
no numerical threshold was adjusted and no completed training was restarted.
The failed attempt's CPU, wall time and memory remain in the campaign cost ledger.
