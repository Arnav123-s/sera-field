# GROW-013: learned residual representation and coupled stationary inference

Prospective integration package in the continuing whole-architecture objective.
Parent is the registered UNIFIED-012 coupled candidate; qualification and all
failure results are carried forward, and the usable CONCEPT-011 remains preserved.
No old final is used for new checkpoint selection.

## Construction

The supplied four-term physical route remains accessible. A conditional network
learns smooth residual features from measured inputs and the actual shared field.
No omitted-force formula or family identity enters this network. Features are
orthogonally residualized against the existing basis on adaptation observations.
Candidate extensions have ranks 4, 8 and 16. These are explicit engineering
capacity bounds, not an assertion of unlimited autonomous allocation.

A finite Gaussian effective action on acquisition coefficients defines its
Hessian, regulator `R_k=k^2 I`, and `d_k Gamma = k Tr((H+k^2 I)^-1)`.
Record three scales, effective rank and observed residuals. The scale diagnostic
does not by itself prove model inadequacy. Held-out calibration observations
qualify an extension if their error falls by at least 25% and absolute baseline
error exceeds 0.015. Prefer the smallest qualifying rank. A failed extension
remains an explicit unresolved goal with its predictions and costs preserved.
Acquisition/calibration observations are distinct from final queried outcomes.

A stationary, stable FHN member supplies a learned boundary transformation.
Eliminate the inhibitor at equilibrium and solve the resulting convex quartic
field equation. Test implicit derivatives against independently unrolled Newton
solutions. This is stationary credit; timed RHEL remains the reversible operator.
The same SPD, bulk and Clifford paths remain downstream, with qualified capture
and the original goal intact.

## Teaching and evaluation

Seed 13113; 2,048 meta-learning updates, batch 16, learning rate 0.0005 for the
new residual representation and stationary gate. Existing parent parameters are
protected in this work package. Each case has 24 adaptation, 8 calibration and
16 teaching-query rows. Hidden simulator families include smooth restoring force,
input saturation and interactions; no family label is provided to the learner.
The curriculum and simulator are supplied; only learned representations and
acquired coefficients are attributed to SERA. No next-token or pretrained loss.

Development every 256 updates, 96 systems, select mean query MSE with ranks
chosen using calibration only. Final once: 256 fresh supported systems and 128
with a newly introduced higher-frequency response; preserve the latter errors.
Baselines are the same protected owner with no extension and a fixed random
feature extension with identical observations and rank choices. No performance
claim about generic model classes or differently trained language models.

Every final acquired model returns forward predictions, inverse bounded-control
solutions, counterfactual differences, a bounded plan and a language-conditioned
direction answer. All five use the identical acquired parameter record, including
its extension. Independent simulator outcomes grade each result. Proposed
solutions with residual error stay conditional and retain all alternatives.

Evaluate correction, restarting a saved acquisition, alternative solutions and
the requested/executed intervention distinction. Test earlier human/exact routes
for retention; if the stationary path changes them outside the UNIFIED-012
retention allowance, keep it as a candidate rather than promote. Independently
replay final records and content identities. One CPU thread, 2 GiB job cap, all
attempts and source identities retained, no paid compute.
