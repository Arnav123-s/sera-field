# INQUIRY-014: complete measurement alternatives and learned local investigation

Freeze before this controller's numerical tests or teaching. Continue the selected
GROW-013 owner; retain its actual qualification outcome. Preserve all earlier
owners and sealed cohorts. This is part of the open whole-architecture task.

## Coupled mechanism

The actual acquired model predicts response and residuals for available
observations. Those values drive a 16-unit recurrent investigation policy.
Its diagonal recurrence supports an exact forward local eligibility calculation;
independent Gaussian action scores supply the global learning signal. Verify these
derivatives against ordinary autograd before teaching. This is a specified
restricted e-prop construction, not a claim about arbitrary recurrent networks.

The policy samples two continuous coordinates. They prepare a logical qubit in a
six-Majorana Ising representation. Three charge measurements, for each of three
data-pair choices, produce all 24 branches. Keep every outcome, probability,
computational cost and source identity. The all-positive branch is checked against
the corresponding braid operator. Merge identical probe coordinates for selection
while preserving their probability and every contributing branch.

Logical observables map to bounded velocity/control probes. This physical-action
map is an engineering choice. The acquired response model supplies each probe's
conditional consequences; the quantum representation supplies no correctness
certificate. No branch postselection or infinite-parallelism claim is permitted.

An actual simulator intervention then returns requested/performed coordinates and
its observed outcome. Re-acquire the relation, qualify extensions on separate
calibration, and return to the same original goal. Independent query error change
supplies policy credit. Positive checked improvement may capture the resulting
field in the same bulk; failed actuation still supplies its actual measurement but
receives no incorrectly scoped reward. Canonical contribution bonuses identify
branch family and extension-rank transition, not every new floating-point query.

## Finite teaching and decision

Seed 14114. Two matched arms: local-credit updates plus qualified capture, and
disconnected policy updates/capture. 1,024 episodes per arm, three performed probes
per episode. Both see the same initial eight adaptation and eight calibration
rows and the same original-goal query cohort. Each arm pays all probe and grading
costs. Learning rate 0.02, score-gradient clip 1, action standard deviation 0.7.
Development every 256 episodes on 64 disjoint cases; choose the lowest final
original-goal MSE, including the initialized candidate in selection.

Final: 128 new supported cases and 64 higher-frequency cases, three probes per
case, source split `INQUIRY-014-final-v1`. Controls: the identically initialized
controller, uniform probes and learned-feature information selection over the
same complete candidate set. All final actors start from the same acquired-model
parent; record differences in retained training memory explicitly. Also evaluate
the selected controller with new training captures disconnected at inference.
Replay selected-policy cases exactly. No final-based retraining or selection.

Report learning effects, returned goals, calibration choices, distinct proposed
and executed paths, actuator mismatches, rejected/stale/duplicate credit, capture,
retention, wall/CPU/memory/storage costs and all failures. Qualification requires
complete source identities and exact replay, every original goal returned, correct
rejection of mismatched credit, finite predictions and retention within the prior
0.03 accuracy allowance. Policy benefit must be measured separately against its
initialized controller. One numerical CPU thread, 2 GiB process-tree cap, exclusive
lease; no paid compute. Save actor eligibility, pending decisions, RNG, optimizer,
owner, memory and sampling position at durable episode boundaries.
