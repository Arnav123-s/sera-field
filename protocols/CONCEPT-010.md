# CONCEPT-010: grounded-question representation repair

Frozen before training. CONCEPT-008 passed four numerical gates but failed
its joint explanation gate. The separate CONCEPT-009 repair improved final joint
direction from 57.42% to 74.41%, with 79.88% routing. Both final cohorts are closed.

The failure motivates a representation extension, not further head-only fitting.
Add 8,194 freshly initialized parameters to the selected CONCEPT-009 owner:
a residual linear map over deterministic signed hashed words and character
2–5-grams (4,096 dimensions, L2 normalization). No variable-name matching rule,
pretrained model or final answer enters the feature map. Freeze every parent
parameter, including the human encoder, core, acquired-model updater, meaning
head and qualified investigation policy.

Compare the repaired owner with unchanged CONCEPT-009, unchanged CONCEPT-008
reward owner and preserved ECHO-007 on the SAME fresh systems. Word and character
features are a targeted alternative to freezing the old representation, based
on [feature hashing](https://arxiv.org/abs/0902.2206). This is an engineered input
representation with learned routing weights, not claimed general language
understanding. Training is supplied annotation teaching for this simulation
interface; report distinct phrases separately from presentations.

Freeze banks generated by concept_language_study.phrases before training:
training paired variable substitutions, 32 development phrases and 80 final
phrases with held-out sentence forms. Exact strings are disjoint. Development
and final share taught variable names; do not call this new-word acquisition.
Retain all earlier phrase banks. No use of either earlier final bank for teaching.

Train 2,048 batches of 32 paired phrases, seed 10110, AdamW lr .01, weight decay
.001, feature noise SD .01, cross-entropy plus .1 paired margin, clip norm 3.
Select maximal development accuracy, then minimal development cross-entropy,
then earliest step, at zero and every 128 updates. Save optimizer and RNG.

Register selected weights BEFORE the fresh 512 supported/128 omitted-system
cohort is opened. Four owners, independent simulation/DOP853 outcomes, same five
downstream views, all four fresh-process exact replays. Keep CONCEPT-009 gates:
>=90% routing, >=80% joint direction, >=10 percentage-point joint direction
gain over the original CONCEPT-008 language owner. Require exact numerical
output retention against CONCEPT-009 and parameter equality for every old weight.
Forward/inverse/counterfactual MSE <=.8 of ECHO-007, plan MSE <=.9 of ECHO-007.
No post-final adjustment. Retain omitted-mechanism diagnostics and all failures.

Verify exact next-update resume and unchanged core/numerical skills. Package
only after qualification. Exercise persistent creation, restart, new tasks,
independent evidence append and returned original goal through the actual CLI.
One CPU thread, exclusive shared lease, 2 GiB process-tree cap, no paid compute.
