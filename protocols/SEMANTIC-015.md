# SEMANTIC-015: human meaning, imagined alternatives and curvature capture

Freeze before numerical experiments. Continue the qualified INQUIRY-014 owner
(the development-selected controller may be its initialized state). Preserve
every earlier owner and its complete results. This package connects human meaning
and actual geometric memory through the existing field; it does not close the
entire research objective.

## Evidence and mathematics

The original SNLI 1.0 archive is
`afb3d70a5af5d8de0d9d81e2637e0fb8c22d1235c2749d83125ca43dab0dbd3e`.
The authors describe human-written sentences and manual annotations. Keep original
sentence text and attribution (Bowman, Angeli, Potts and Manning, 2015;
CC BY-SA 4.0). Image/caption groups are disjoint across the local partitions;
76,180 official training pairs were removed because their source groups were
reserved. The remaining training bank has 473,187 pairs. Labels concern what
follows from the stated premise, not whether the premise occurred in reality.

For transported local vectors, define context-dependent connections
`T_i = R_i exp(epsilon [x_i]_cross)` from node i to i+1. For each base node i,
multiply one closed ring in order to obtain `H_i`. The antisymmetric part
`(H_i-H_i^T)/2` gives a discrete holonomy-curvature tag by the axial map.
Under local rotations Q_i, `H_i -> Q_i H_i Q_i^T` and the tag transforms as a
local vector. This is a finite closed-loop construction, not the claim that a
generic activation is the continuum tensor F_mu_nu. Test covariance, flat zero
curvature, finite derivatives and actual connected retrieval.

A separate typed region in the existing owner's condensate bulk retains these
curvature tags. Earlier activation-tag memories stay typed and unchanged. Its
retrieval modifies the same boundary input before covariance, stationary and
temporal-echo processing. The inherited Chern density codec and guarded capture
remain. Periodic conserved relaxation records energy, mass and phase identities.
Finite quantization error must be reported; it does not imply universal invariance.

## Learned meaning and finite teaching

The retained from-scratch word encoder reads a human premise and hypothesis.
An observed boundary state is settled first. Three learned conditional boundary
perturbations then continue that same state, and the readout uses their actual
field states to predict entailment, contradiction or unresolved information.
There is no next-token objective and no imported pretrained model. Semantic labels
never enter the input field. Three latent branches are supplied architecture;
their weights are learned, and branch count alone earns no novelty credit.

Seed 15115. Reserve 2,048 training IDs by deterministic hash for checked capture
practice. Teach every other eligible human training pair once, shuffled, batches
of 32. After each four semantic batches, rotate a batch of 16 through retained
reading, mathematics, source pairs (dictionary, conversation, grammar, philosophy,
calculus, code) and independent physical simulation. Save exact sampler/RNG,
optimizer, local credit, memory and owner state every 256 total updates.

Train new semantic parameters, curvature coupling and capture policy first. After
1,024 semantic batches, also train the existing word encoder with rehearsal;
earlier arithmetic/program primitives and physical-operator parameters stay fixed.
Use AdamW, new-parameter learning rate 0.001, old word-encoder rate 0.0001,
weight decay 0.0001 and gradient norm cap 1. Semantic loss is cross entropy on
deterministic bootstrap membership for the three conditional branches.

Every 128 updates, propose a curvature capture using a training-source tag.
Assess the conditional memory change on 12 separate human-annotated capture-practice
records. Reward reduction in checked log loss with no reduction in accuracy.
Qualification is scoped to annotation-relative semantic performance. Independent
human annotations, actual proposal/predictor/goal identities, source hashes and
duplicate-credit guards remain mandatory. Only an accepted positive proposal is
captured. Consolidate each typed bulk region every 256 updates, logging its costs
and conservation diagnostics. Human text remains unchanged on disk.

## Development, final and acceptance

Evaluate 512 fixed development pairs every 1,024 semantic batches plus the final
training boundary. Use only this development accuracy for selection, with lower
log loss breaking ties. Preserve the initialized semantic head and every attempted
update. Development selection cannot use final or retention outcomes.

Final: 4,096 unopened official test pairs, selected by hash before opening labels.
Controls are the initialized semantic head, no imagined continuation, disconnected
curvature retrieval, and hypothesis-only input. These inference ablations measure
dependence within this owner, not equal-training architectural superiority.
Report accuracy, per-label confusion, loss, calibration, branch agreement and
disagreement. Include source-group identities, exact fresh-process replay and
all teaching/capture/relaxation costs. Final predictions must not update weights.

Prospective regression: the earlier opened UNIFIED-012 retention cohort (0.03
accuracy allowance), retained exact operator tests and 128 new five-use physical
systems against the qualified parent (forward error no more than 1.15 times the
parent). No regression row trains or selects the semantic candidate. If a gate
fails, preserve the complete candidate and use a new repair protocol/cohort.

Test persistent original-goal and factual/conditional separation through the
existing inquiry session. Research goals still open include general concept
construction, literal ambient-space/sheaf growth, broader observer and literary
interpretation, and independently improved learning procedures. One CPU numerical
thread, 2 GiB process tree, exclusive shared lease, no paid services.
