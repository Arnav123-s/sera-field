# SCFE-004: implement and train the new addendum through the actual owner

Prospectively frozen before any SCFE training or final scoring. This adopts the
new research as hypotheses and engineering objectives. It preserves CONNECTED-003
and adds a freshly initialized Clifford/sheaf candidate from the first update.

Use the same seed 1103, full human source partitions, duplicate quarantine,
12,288 attempted minibatches, batch 24, source permutations, simulation indices,
schedule, teaching optimizer, checkpoint and development-selection rule as
CONNECTED-003. Only the finite field substrate changes. The constructor initializes
all word/task weights afresh and copies geometric coordinates only from its own
fresh initialization; it imports no trained model. Compare equal actual teaching
exposure. Record changed parameter/state counts; do not claim exact total-capacity
matching or that this single-seed study establishes broad superiority.

## Implemented mathematical scope

Real Cl(3,0) multivectors, the geometric product and grade-preserving Spin(3)
transport on a graph sheaf. Edge discrepancy is delta psi; the actual action
gradient is delta^T delta psi, which transports discrepancies back into the proper
vertex frames. A quartic radial condensation term and learned persistent prior
couple to the boundary. Bounded backtracking checks actual action decrease.
The existing Wilson feature remains in the forward path. This is a finite graph
and numerical action, not a derived AdS/CFT system, an anyonic computer, a neural
Wetterich dimension constructor or a topological mathematical verifier.

The sample's arbitrary SO(8) matrix is tested against preservation of Clifford
products and grades. The repaired field is tested for basis algebra, rotor action,
local-frame covariance, action derivatives, actual shared-owner gradients and
non-mutating hypothetical computation. Numeric coefficients are stored in arrays;
geometric meaning is supplied by their algebra and transport rules.

## Behavioral evaluation and retention

For each owner, train the same 2,048-episode verified-policy arm and the same
reward-disconnected arm, with one independent probe per attempted episode. Keep
the taught predictor fixed during that mechanistic reward comparison. Report
negative progress, actuation failures and actual policy changes.

Before final opening, freeze both teacher selections and reward-arm identities.
Use the predetermined CONNECTED-003 final groups for a registered, simultaneous
two-owner comparison; evaluator artifacts get distinct output roots. No further
selection or model repair may use those scores. A separate unopened cohort stays
reserved. Independent replay must match exact raw records. Human reading,
arithmetic proposals, source/program association, physical consequences, acquisition
and returned original goals retain separate metrics.

Add 64 one-second motion transfers: learned acceleration hypotheses drive RK4
rollouts; a separately coded DOP853 environment supplies outcome checks. This
extension is frozen before any final trajectories are computed. These are finite
simulated trajectory checks, not a proof of a physical law.

## Preserved research obligations

The original paper and latest additions remain immutable in intake. Record all
reference corrections and rows for Grassmannians, topology/winding, holography,
ERG/growth and e-prop. A phase potential does not close those rows. Independent
verification remains required for factual credit. Report full owner weights,
working states, optimizer/traces and process-tree memory separately; a small core
state does not establish that the complete language learner fits 35,840 bytes.
